pluginManagement {
	repositories {
		maven {
			name = 'Fabric'
			url = 'https://maven.fabricmc.net/'
		}
		mavenCentral()
		gradlePluginPortal()
	}
}

buildCache {
	local {
        directory = file("build-cache")
	}
}

rootProject.name = "sil_yoni"

include("private-content")
